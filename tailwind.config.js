const colors = require('tailwindcss/colors')

module.exports = {
  content: ["./src/**/*.{html,js}"],
  theme: {
    extend: {
      fontSize: {
      },
      colors: {
      },
      borderWidth: {
        '1/2': 0.5
      }
    },
  },
  plugins: [],
}
